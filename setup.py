#!/usr/bin/env python

from setuptools import setup

setup(
    name='{Game_of_life}',
    version='1.0',
    author='{dianevrb}',
    license='MIT',
    long_description=open('README.md').read(),
    install_requires=[
        'numpy',
    ],
)